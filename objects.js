var playlist = {
  artist: "title"
}

function updatePlaylist(playlist, artistName, songTitle) {
 returnlearn tes Object.assign({}, playlist, { [artistName]: songTitle})
}

function removeFromPlaylist(playlist, artistName) {
delete playlist.Kanye
 return playlist
}
